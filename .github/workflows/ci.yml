name: CI

on:
  push:
    branches: [main, "claude/**"]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  flutter:
    name: Flutter analyze & test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.27.x"
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Generate localisations
        run: flutter gen-l10n
      - name: Analyze
        # Errors and warnings gate the build; style infos (e.g. trailing commas,
        # which `dart format` maintains) are reported but non-fatal.
        run: flutter analyze --no-fatal-infos
      - name: Test
        run: flutter test --coverage
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage/lcov.info

  build-android:
    name: Build Android APK
    # Manual only. This used to run on every push, publishing a new APK to the
    # GitHub release on each one — with pushes going to both the feature branch
    # and main, that silently piled up dozens of stale release assets. Trigger
    # this from the Actions tab (or ask for it) only when a sideload build is
    # actually needed.
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.27.x"
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Generate localisations
        run: flutter gen-l10n
      # Release signing is optional: if the four ANDROID_* secrets below are
      # configured on this repo, we reconstruct key.properties + the upload
      # keystore from them and android/app/build.gradle picks up real release
      # signing automatically. Without them, it falls back to debug signing —
      # installable for sideload testing but not for Play Store upload.
      - name: Check for release signing secrets
        id: keystore_check
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Restore release keystore
        if: steps.keystore_check.outputs.present == 'true'
        # Secrets go through `env` rather than straight into the script via
        # ${{ }} — a password containing a `"` or `$` would otherwise break
        # out of the echo/heredoc and corrupt key.properties.
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/upload-keystore.jks
          cat > android/key.properties <<EOF
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=upload-keystore.jks
          EOF
      - name: Build release APK
        run: flutter build apk --release
      # Play Console requires an App Bundle for new app submissions — a raw
      # APK alone can't be uploaded as the initial release. Same signing
      # config as the APK above, just a different packaging format.
      - name: Build release App Bundle
        run: flutter build appbundle --release
      - name: Rename artifacts
        run: |
          mkdir -p dist
          cp build/app/outputs/flutter-apk/app-release.apk dist/robux-box-latest.apk
          cp build/app/outputs/bundle/release/app-release.aab dist/robux-box-latest.aab
      # A single rolling "android-latest" release: delete it first (ignoring
      # "not found" on the very first run) and recreate it fresh, so exactly one
      # set of artifacts ever exists instead of accumulating new assets on
      # every run.
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete android-latest --yes --cleanup-tag || true
          if [ "${{ steps.keystore_check.outputs.present }}" = "true" ]; then
            gh release create android-latest dist/robux-box-latest.apk dist/robux-box-latest.aab \
              --target "${{ github.sha }}" \
              --title "Robux Box (Android) — latest" \
              --notes "Automated build from ${{ github.sha }} on ${{ github.ref_name }}. Release-signed with the upload key. Upload the .aab to Play Console for store submission — the .apk is for direct/sideload installs."
          else
            gh release create android-latest dist/robux-box-latest.apk dist/robux-box-latest.aab \
              --target "${{ github.sha }}" \
              --title "Robux Box (Android) — latest" \
              --notes "Automated build from ${{ github.sha }} on ${{ github.ref_name }}. Debug-signed for sideload testing only — not signed for Play Store upload." \
              --prerelease
          fi

  functions:
    name: Cloud Functions build & test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - run: npm test
